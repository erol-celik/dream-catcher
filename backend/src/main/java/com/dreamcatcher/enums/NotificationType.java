package com.dreamcatcher.enums;

/**
 * Types of local push notifications scheduled by the app.
 * These are time-based statistical notifications, not sensor-driven.
 */
public enum NotificationType {
    MORNING_REMINDER,
    EVENING_REMINDER,
    WEEKLY_REPORT,
    COMMUNITY
}
