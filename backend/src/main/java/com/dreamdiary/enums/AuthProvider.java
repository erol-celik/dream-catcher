package com.dreamdiary.enums;

/**
 * Authentication provider types supported by the application.
 * Only social logins are allowed; no SMS OTP.
 */
public enum AuthProvider {
    GOOGLE,
    APPLE
}
